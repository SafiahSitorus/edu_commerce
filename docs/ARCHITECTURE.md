# 🏗️ ARCHITECTURE OVERVIEW

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         BROWSER / CLIENT                         │
└─────────────────────────────────────────────────────────────────┘
                                ▲
                                │
┌─────────────────────────────────────────────────────────────────┐
│                    NEXT.JS APP (Frontend)                        │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                     PRESENTATION LAYER                       │ │
│ │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │ │
│ │  │  Login Page  │  │  Dashboard   │  │  Other Pages │      │ │
│ │  │   /login     │  │  /dashboard  │  │    /...      │      │ │
│ │  └──────────────┘  └──────────────┘  └──────────────┘      │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                │                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                     COMPONENT LAYER                          │ │
│ │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │ │
│ │  │  LoginForm   │  │  UserCard    │  │  UI Comps    │      │ │
│ │  │  Component   │  │  Component   │  │  (shadcn)    │      │ │
│ │  └──────────────┘  └──────────────┘  └──────────────┘      │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                │                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                  STATE MANAGEMENT LAYER                      │ │
│ │                   (REDUX TOOLKIT)                            │ │
│ │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │ │
│ │  │  Auth Slice  │  │ Other Slices │  │    Hooks     │      │ │
│ │  │  - user      │  │  - products  │  │ useDispatch  │      │ │
│ │  │  - token     │  │  - orders    │  │ useSelector  │      │ │
│ │  │  - loading   │  │  - ...       │  │              │      │ │
│ │  └──────────────┘  └──────────────┘  └──────────────┘      │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                │                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                     SERVICE LAYER                            │ │
│ │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │ │
│ │  │Auth Service  │  │User Service  │  │Other Services│      │ │
│ │  │ - loginUser  │  │ - getProfile │  │ - getData    │      │ │
│ │  │ - logout     │  │ - updateUser │  │ - ...        │      │ │
│ │  └──────────────┘  └──────────────┘  └──────────────┘      │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                │                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │                     HTTP CLIENT LAYER                        │ │
│ │                    (AXIOS + INTERCEPTORS)                    │ │
│ │  ┌─────────────────────────────────────────────────────────┐│ │
│ │  │  Request Interceptor: Add Bearer Token                   ││ │
│ │  └─────────────────────────────────────────────────────────┘│ │
│ │  ┌─────────────────────────────────────────────────────────┐│ │
│ │  │  Response Interceptor: Handle Errors & 401               ││ │
│ │  └─────────────────────────────────────────────────────────┘│ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND API SERVER                            │
│                    https://api.edusav.com/api                    │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagrams

### 1. Login Flow

```
┌──────────┐
│  User    │
│  Input   │
└────┬─────┘
     │
     ▼
┌─────────────────┐
│  LoginForm      │ ◄── Form Validation (Zod)
│  Component      │
└────┬────────────┘
     │ onSubmit
     ▼
┌─────────────────┐
│ useAppDispatch  │
│ loginUser()     │ ◄── Redux Thunk
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Auth Slice      │ ◄── Set isLoading: true
│ (Redux)         │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Auth Service    │ ◄── API Call
│ loginUser()     │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Axios Client    │ ◄── HTTP POST
│ (api-client)    │     /auth/loginUser
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Backend API     │ ◄── Authenticate
└────┬────────────┘
     │
     │ Response: { token, user }
     ▼
┌─────────────────┐
│ Auth Service    │ ◄── Return data
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Auth Slice      │ ◄── Update state
│ - user: {...}   │     Save to localStorage
│ - token: "..."  │
│ - isAuth: true  │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Component       │ ◄── Re-render
│ Redirect        │     → /dashboard
└─────────────────┘
```

### 2. Protected Route Flow

```
┌──────────┐
│  User    │
│  Access  │
│ /dashboard
└────┬─────┘
     │
     ▼
┌─────────────────┐
│ Dashboard       │
│ Component       │
└────┬────────────┘
     │ useEffect
     ▼
┌─────────────────┐
│ useAppSelector  │ ◄── Get Redux state
│ auth state      │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│ Check           │
│ isAuthenticated │
└────┬────────────┘
     │
     ├─ YES ──────────────┐
     │                    │
     │                    ▼
     │            ┌──────────────┐
     │            │ Show Content │
     │            └──────────────┘
     │
     └─ NO ───────────────┐
                          │
                          ▼
                  ┌──────────────┐
                  │ Redirect to  │
                  │   /login     │
                  └──────────────┘
```

### 3. API Request with Token

```
┌──────────────┐
│ Component    │
│ Makes API    │
│ Request      │
└───┬──────────┘
    │
    ▼
┌──────────────┐
│ Service      │
│ Method       │
└───┬──────────┘
    │
    ▼
┌──────────────────────────┐
│ Axios Client             │
│ Request Interceptor      │
└───┬──────────────────────┘
    │
    │ 1. Get token from localStorage
    │ 2. Add Authorization header
    │    "Bearer {token}"
    │
    ▼
┌──────────────┐
│ HTTP Request │──────►  Backend API
│ with Token   │
└───┬──────────┘
    │
    │ Response
    ▼
┌──────────────────────────┐
│ Axios Client             │
│ Response Interceptor     │
└───┬──────────────────────┘
    │
    ├─ Status 200 ─────┐
    │                  │
    │                  ▼
    │         ┌───────────────┐
    │         │ Return Data   │
    │         └───────────────┘
    │
    ├─ Status 401 ─────┐
    │                  │
    │                  ▼
    │         ┌───────────────┐
    │         │ Clear Token   │
    │         │ Redirect Login│
    │         └───────────────┘
    │
    └─ Other Error ────┐
                       │
                       ▼
              ┌───────────────┐
              │ Return Error  │
              └───────────────┘
```

## Technology Stack

### Frontend Framework
```
Next.js 15 (App Router)
├── React 19
├── TypeScript 5
└── Server/Client Components
```

### Styling
```
Tailwind CSS v4
└── shadcn/ui Components
    ├── Button
    ├── Input
    ├── Card
    ├── Form
    └── Label
```

### State Management
```
Redux Toolkit
├── Slices (Feature-based)
├── Async Thunks
├── Typed Hooks
└── Store Configuration
```

### HTTP Client
```
Axios
├── Request Interceptors
├── Response Interceptors
└── Base Configuration
```

### Form & Validation
```
React Hook Form
└── Zod Schema Validation
```

### Storage
```
LocalStorage
├── Token Management
└── User Data Persistence
```

## Key Architectural Patterns

### 1. Separation of Concerns

```
Components (UI)
    ↕
Redux Store (State)
    ↕
Services (Business Logic)
    ↕
API Client (HTTP)
    ↕
Backend API
```

### 2. Feature-Based Structure

```
auth/
├── components/     LoginForm.tsx
├── services/       auth.service.ts
├── store/          authSlice.ts
└── types/          auth.ts
```

### 3. Dependency Injection

```typescript
// Services use api-client
import apiClient from './api-client';

// Components use services
import { authService } from '@/services/auth.service';

// Components use Redux
import { useAppDispatch } from '@/store/hooks';
```

### 4. Single Responsibility

```
Component    → UI Rendering
Service      → API Communication
Redux Slice  → State Management
Utility      → Helper Functions
```

## Security Architecture

### Token Management
```
┌─────────────────┐
│  Login Success  │
└────────┬────────┘
         │
         ▼
┌──────────────────────────┐
│ Store Token & User Data  │
│ ├─ localStorage          │
│ └─ Redux State           │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ All API Requests         │
│ Include Bearer Token     │
│ (via Interceptor)        │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│ On 401 Response          │
│ ├─ Clear token           │
│ ├─ Clear user data       │
│ └─ Redirect to login     │
└──────────────────────────┘
```

### Environment Security
```
Development   → .env.local
Staging       → .env.staging
Production    → .env.production
              ↓
        Runtime Validation
              ↓
        Type-Safe Access
```

## Performance Optimization

### Code Splitting
```
Next.js App Router
├── Automatic route-based splitting
├── Dynamic imports for heavy components
└── Server/Client component optimization
```

### State Management
```
Redux Toolkit
├── Memoized selectors
├── Normalized state
└── Efficient updates
```

### Rendering
```
React 19
├── Server Components (default)
├── Client Components ('use client')
└── Streaming & Suspense
```

## Scalability Considerations

### Adding Features
1. Create feature folder
2. Add service layer
3. Create Redux slice
4. Build components
5. Add to routes

### Adding API Endpoints
1. Add method to service
2. Create types if needed
3. Add Redux thunk if needed
4. Use in components

### Adding Pages
1. Create folder in `app/`
2. Add `page.tsx`
3. Optional: Add layout
4. Connect to Redux if needed

## Error Handling Strategy

```
Component Level
    │ Try/Catch
    ▼
Redux Level
    │ Async Thunk Error
    ▼
Service Level
    │ API Error
    ▼
Axios Interceptor
    │ Global Handler
    ▼
User Feedback
    │ Error Display
```

## Development vs Production

### Development
```
- Hot reload enabled
- Source maps
- Detailed errors
- Dev tools accessible
- .env.local
```

### Production
```
- Optimized build
- Minified code
- Error tracking
- Performance monitoring
- .env.production
```

---

## Best Practices Applied

✅ **Single Source of Truth** - Redux for state
✅ **Immutable State** - Redux Toolkit
✅ **Type Safety** - TypeScript everywhere
✅ **Separation of Concerns** - Layered architecture
✅ **DRY Principle** - Reusable components/services
✅ **Error Handling** - Multiple levels
✅ **Security** - Token management, validation
✅ **Scalability** - Feature-based structure
✅ **Maintainability** - Clear organization
✅ **Documentation** - Comprehensive guides

